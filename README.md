# 💬 Multi-Client Chat System (TCP Sockets)

A simple Python-based real-time chat application using **TCP** and **multi-threading**. Multiple clients can connect to a single server and exchange messages concurrently.

---

## 🚀 Features
- **TCP Protocol** for reliable data transfer  
- **Multi-threaded Server** handles multiple clients  
- **Broadcast System** — messages are sent to all connected users  
- **Asynchronous Client** for simultaneous send/receive  
- **Graceful Disconnect** with `q` or `!DISCONNECT`

---

## 🛠️ Requirements
- Python 3.x  
- Two or more terminal windows  

---

## ⚙️ Run
1. **Start Server**
   ```bash
   python server.py
Output: [LISTENING] Server is listening on localhost:5050

Start Clients
  ```bash
   python server.py


